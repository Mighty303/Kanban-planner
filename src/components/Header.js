import React from 'react';

const Header = props => {
    return (
        <header>
            <h1>Kanban Planner</h1>
            <p>Plan ahead, stay ahead</p>        
        </header>
    );
};

export default Header;